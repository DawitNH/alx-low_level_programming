#include "main.h"
/**
 * add - Adds two integers
 *
 * @num1: the first number
 * @num2: the second number
 * Return: the result
 */
int add(int num1, int num2)
{
	int res;

	res = num1 + num2;
	return (res);
}
