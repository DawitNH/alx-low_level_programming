#include <stdio.h>
#include <string.h>
/**
 * main - prints string
 *
 * Return: 0 exits the program
 */
int main(void)
{
	char str[200];

	strcpy(str, "Programming is like building a multilingual puzzle");
	puts(str);
	return (0);
}
